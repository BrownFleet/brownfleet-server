import { Router } from 'express';
import { mcpController } from '../controllers/mcp.controller';

const router = Router();

// router.post('/generate', mcpController.generateResponse.bind(mcpController));
// router.post('/analyze', mcpController.analyzeText.bind(mcpController));

export default router;